import React from "react";
import StudentItem from "./StudentItem";

type Student = {
    id: number;
    name: string;
    present: boolean;
};



const StudentList = () => {
    return ();
};

export default StudentList;